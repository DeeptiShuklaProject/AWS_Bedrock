# Episode 7 Summary: Local Values and Dynamic Expressions

Locals act like temporary local variables inside your module. They allow you to avoid repeating complex expressions.

## Production Design
- Combine multiple variables into a single tag map (`local.common_tags`) to enforce tagging compliance.
- Keep naming structures DRY (Don't Repeat Yourself).
