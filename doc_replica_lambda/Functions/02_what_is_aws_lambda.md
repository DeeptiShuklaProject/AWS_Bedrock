# Section 2 – What is AWS Lambda?

<a name="sec-2"></a>

AWS Lambda is a serverless, event-driven compute service that runs code in response to triggers without managing servers.

### Event-Driven Computing & Function as a Service (FaaS)
Lambda sits at the core of event-driven architectures. Instead of continuously running a process to check for work (polling), Lambda remains idle until an **Event** occurs.
An event is a structured JSON payload representing a state change, generated by an event source (such as S3, API Gateway, or SQS).

### How Lambda Works Internally
When an event triggers a function, the Lambda control plane coordinates the execution:

```
[User/Service Event] ──► [Lambda Trigger] ──► [Provision Firecracker MicroVM] ──► [Load Runtime & Handler] ──► [Return Response]
```

1. **Trigger**: An event source calls the Lambda invoke API.
2. **Execution Environment**: Lambda provisions a secure container environment using a **Firecracker MicroVM**.
3. **Bootstrap**: The execution environment initializes the selected runtime (e.g., Python 3.12) and loads the handler code.
4. **Invocation**: The handler runs with the event payload.
5. **Freeze/Thaw**: After execution, the environment is frozen for reuse in subsequent warm starts.

### Cold Start vs Warm Start
* **Cold Start**: Occurs when a function is invoked for the first time or after a period of inactivity. AWS must create a new MicroVM instance, initialize the runtime, and compile/load files. This introduces an initial delay (from 100ms up to a few seconds).
* **Warm Start**: Occurs when a subsequent execution uses an already initialized, active MicroVM instance. Warm starts bypass initialization, executing the handler function instantly.
* **Statelessness**: Because instances are created and destroyed dynamically, local disk storage `/tmp` and global memory state are not guaranteed to persist across different invocations.

---
